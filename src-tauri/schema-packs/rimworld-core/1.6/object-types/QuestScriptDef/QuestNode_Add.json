{
  "objectType": "QuestNode_Add",
  "label": "QuestNode Add",
  "description": "Schema for RimWorld.QuestGen.QuestNode_Add.",
  "inherits": [
    "QuestNode"
  ],
  "fieldOrder": [
    "value1",
    "value2",
    "storeAs"
  ],
  "fields": {
    "value1": {
      "description": "SlateRef<double> first numeric operand to add; accepts literal, expression, or $varName.",
      "type": {
        "kind": "string"
      },
      "required": false,
      "xml": "element"
    },
    "value2": {
      "description": "SlateRef<double> second numeric operand to add; accepts literal, expression, or $varName.",
      "type": {
        "kind": "string"
      },
      "required": false,
      "xml": "element"
    },
    "storeAs": {
      "description": "SlateRef<string> variable name for the numeric result.",
      "type": {
        "kind": "string"
      },
      "required": false,
      "xml": "element"
    }
  }
}
