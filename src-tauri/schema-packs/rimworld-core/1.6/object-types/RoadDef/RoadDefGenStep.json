{
  "objectType": "RoadDefGenStep",
  "label": "Road gen step",
  "description": "Base schema for road generation steps. The Class attribute selects the concrete subclass applied during map generation.",
  "fieldOrder": ["Class", "chancePerPositionCurve", "antialiasingMultiplier", "periodicSpacing"],
  "discriminator": {
    "attribute": "Class",
    "fallbackSchemaRef": "RoadDefGenStep",
    "allowMissing": false,
    "allowUnknown": true,
    "variants": {
      "RoadDefGenStep_Bulldoze": "RoadDefGenStep_Bulldoze",
      "RoadDefGenStep_DryWithFallback": "RoadDefGenStep_DryWithFallback",
      "RoadDefGenStep_Place": "RoadDefGenStep_Place"
    }
  },
  "fields": {
    "Class": {
      "label": "Class",
      "description": "C# RoadDefGenStep subclass that implements this generation step.",
      "type": { "kind": "typeName" },
      "required": false,
      "xml": "attribute"
    },
    "chancePerPositionCurve": {
      "label": "Chance per position curve",
      "description": "Piecewise linear curve mapping distance from the road center (x) to placement probability (y) for each map cell.",
      "type": { "kind": "object", "schemaRef": "SimpleCurve" },
      "required": false,
      "xml": "object"
    },
    "antialiasingMultiplier": {
      "label": "Antialiasing multiplier",
      "description": "Scales how far the placement probability extends past the hard road edge to produce smooth borders. Set to 0 to disable antialiasing for this step.",
      "type": { "kind": "float" },
      "required": false,
      "defaultValue": 1,
      "xml": "element"
    },
    "periodicSpacing": {
      "label": "Periodic spacing",
      "description": "When non-zero, placement is restricted to positions that are multiples of this distance along the road, producing evenly spaced decorations.",
      "type": { "kind": "integer" },
      "required": false,
      "defaultValue": 0,
      "xml": "element"
    }
  }
}
