{
  "objectType": "SoundParamTarget_PropertyHighPass",
  "label": "Sound param target: high-pass filter property",
  "description": "Applies the mapped parameter value to a property of the high-pass audio filter.",
  "inherits": ["SoundParamTarget"],
  "fieldOrder": ["Class", "filterProperty"],
  "fields": {
    "filterProperty": {
      "label": "Filter property",
      "description": "Which high-pass filter property to modulate.",
      "type": { "kind": "enum" },
      "required": false,
      "xml": "element",
      "validationHints": {
        "allowedValues": ["Cutoff", "Resonance"]
      }
    }
  }
}
