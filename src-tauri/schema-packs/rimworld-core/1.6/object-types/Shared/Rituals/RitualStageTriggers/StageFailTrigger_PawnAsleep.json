{
  "objectType": "StageFailTrigger_PawnAsleep",
  "label": "Stage fail trigger (pawn asleep)",
  "description": "Fails the stage when the specified role pawn falls asleep.",
  "fieldOrder": ["Class", "desc", "allowanceTicks", "pawnId"],
  "fields": {
    "Class": { "label": "Class", "type": { "kind": "typeName" }, "required": false, "xml": "attribute" },
    "desc": { "label": "Description", "type": { "kind": "localizedString" }, "required": false, "xml": "element" },
    "allowanceTicks": {
      "label": "Allowance ticks",
      "description": "Number of ticks the condition is allowed to persist before the stage fails.",
      "type": { "kind": "integer" },
      "required": false,
      "defaultValue": 0,
      "xml": "element"
    },
    "pawnId": {
      "label": "Pawn ID",
      "description": "ID of the role pawn that must remain awake.",
      "type": { "kind": "string" },
      "required": false,
      "xml": "element"
    }
  }
}
