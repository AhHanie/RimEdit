{
  "objectType": "ConditionalStatAffecter_NotInSpace",
  "label": "Conditional stat affecter (not in space)",
  "description": "Applies stat modifications when the pawn is not in a space biome.",
  "fieldOrder": ["Class", "statFactors", "statOffsets"],
  "fields": {
    "Class": {
      "label": "Class",
      "description": "C# ConditionalStatAffecter subclass.",
      "type": { "kind": "typeName" },
      "required": false,
      "xml": "attribute"
    },
    "statFactors": {
      "label": "Stat factors",
      "description": "Stat multipliers applied when not in space.",
      "type": { "kind": "list" },
      "items": { "kind": "object", "schemaRef": "StatModifier" },
      "required": false,
      "xml": "listOfLi"
    },
    "statOffsets": {
      "label": "Stat offsets",
      "description": "Stat additive offsets applied when not in space.",
      "type": { "kind": "list" },
      "items": { "kind": "object", "schemaRef": "StatModifier" },
      "required": false,
      "xml": "listOfLi"
    }
  }
}
