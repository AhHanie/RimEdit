{
  "objectType": "StorytellerCompProperties_NoxiousHaze",
  "label": "Noxious haze comp",
  "description": "Drives adaptive noxious haze events (Biotech DLC) based on how long since the last firing.",
  "fieldOrder": [
    "Class",
    "compClass",
    "minDaysPassed",
    "allowedTargetTags",
    "disallowedTargetTags",
    "minIncChancePopulationIntentFactor",
    "enableIfAnyModActive",
    "disableIfAnyModActive",
    "lastFireMinMTBThreshold",
    "lastFireMaxMTBThreshold"
  ],
  "fields": {
    "Class": {
      "label": "Properties class",
      "description": "C# StorytellerCompProperties type used by RimWorld DirectXmlLoader for this list item.",
      "type": { "kind": "typeName" },
      "required": false,
      "xml": "attribute"
    },
    "compClass": {
      "label": "Component class",
      "description": "Runtime StorytellerComp class.",
      "type": { "kind": "typeName" },
      "required": false,
      "xml": "element"
    },
    "minDaysPassed": {
      "label": "Min days passed",
      "description": "Earliest in-game day on which this comp can fire.",
      "type": { "kind": "float" },
      "required": false,
      "defaultValue": 0,
      "xml": "element"
    },
    "allowedTargetTags": {
      "label": "Allowed target tags",
      "description": "Restrict this comp to firing at incident targets with one of these tags.",
      "type": { "kind": "list" },
      "items": { "kind": "defReference", "reference": { "defType": "IncidentTargetTagDef", "allowAbstract": false, "scope": "allSources" } },
      "required": false,
      "xml": "listOfLi"
    },
    "disallowedTargetTags": {
      "label": "Disallowed target tags",
      "description": "Exclude incident targets with any of these tags.",
      "type": { "kind": "list" },
      "items": { "kind": "defReference", "reference": { "defType": "IncidentTargetTagDef", "allowAbstract": false, "scope": "allSources" } },
      "required": false,
      "xml": "listOfLi"
    },
    "minIncChancePopulationIntentFactor": {
      "label": "Min inc chance population intent factor",
      "description": "Minimum population-intent multiplier applied to incident chance.",
      "type": { "kind": "float" },
      "required": false,
      "defaultValue": 0.05,
      "xml": "element"
    },
    "enableIfAnyModActive": {
      "label": "Enable if any mod active",
      "description": "This comp is active only when at least one mod in this list is loaded.",
      "type": { "kind": "list" },
      "items": { "kind": "string" },
      "required": false,
      "xml": "listOfLi"
    },
    "disableIfAnyModActive": {
      "label": "Disable if any mod active",
      "description": "This comp is disabled when any mod in this list is loaded.",
      "type": { "kind": "list" },
      "items": { "kind": "string" },
      "required": false,
      "xml": "listOfLi"
    },
    "lastFireMinMTBThreshold": {
      "label": "Last fire min MTB threshold",
      "description": "Fraction of the incident MTB that must have elapsed since last firing before the event can fire again (lower bound).",
      "type": { "kind": "float" },
      "required": false,
      "defaultValue": 0.5,
      "xml": "element"
    },
    "lastFireMaxMTBThreshold": {
      "label": "Last fire max MTB threshold",
      "description": "Fraction of the incident MTB after which the event is guaranteed to fire (upper bound).",
      "type": { "kind": "float" },
      "required": false,
      "defaultValue": 2,
      "xml": "element"
    }
  }
}
