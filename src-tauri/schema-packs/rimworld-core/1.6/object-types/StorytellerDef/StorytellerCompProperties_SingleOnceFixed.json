{
  "objectType": "StorytellerCompProperties_SingleOnceFixed",
  "label": "Single once fixed comp",
  "description": "Fires a specific incident exactly once after a fixed day delay. Used for tutorial milestones and Royal summons.",
  "fieldOrder": [
    "Class",
    "compClass",
    "minDaysPassed",
    "allowedTargetTags",
    "disallowedTargetTags",
    "minIncChancePopulationIntentFactor",
    "enableIfAnyModActive",
    "disableIfAnyModActive",
    "incident",
    "fireAfterDaysPassed",
    "skipIfColonistHasMinTitle",
    "skipIfOnExtremeBiome",
    "minColonistCount"
  ],
  "fields": {
    "Class": {
      "label": "Properties class",
      "description": "C# StorytellerCompProperties type used by RimWorld DirectXmlLoader for this list item.",
      "type": { "kind": "typeName" },
      "required": false,
      "xml": "attribute"
    },
    "compClass": {
      "label": "Component class",
      "description": "Runtime StorytellerComp class.",
      "type": { "kind": "typeName" },
      "required": false,
      "xml": "element"
    },
    "minDaysPassed": {
      "label": "Min days passed",
      "description": "Earliest in-game day on which this comp can fire.",
      "type": { "kind": "float" },
      "required": false,
      "defaultValue": 0,
      "xml": "element"
    },
    "allowedTargetTags": {
      "label": "Allowed target tags",
      "description": "Restrict this comp to firing at incident targets with one of these tags.",
      "type": { "kind": "list" },
      "items": { "kind": "defReference", "reference": { "defType": "IncidentTargetTagDef", "allowAbstract": false, "scope": "allSources" } },
      "required": false,
      "xml": "listOfLi"
    },
    "disallowedTargetTags": {
      "label": "Disallowed target tags",
      "description": "Exclude incident targets with any of these tags.",
      "type": { "kind": "list" },
      "items": { "kind": "defReference", "reference": { "defType": "IncidentTargetTagDef", "allowAbstract": false, "scope": "allSources" } },
      "required": false,
      "xml": "listOfLi"
    },
    "minIncChancePopulationIntentFactor": {
      "label": "Min inc chance population intent factor",
      "description": "Minimum population-intent multiplier applied to incident chance.",
      "type": { "kind": "float" },
      "required": false,
      "defaultValue": 0.05,
      "xml": "element"
    },
    "enableIfAnyModActive": {
      "label": "Enable if any mod active",
      "description": "This comp is active only when at least one mod in this list is loaded.",
      "type": { "kind": "list" },
      "items": { "kind": "string" },
      "required": false,
      "xml": "listOfLi"
    },
    "disableIfAnyModActive": {
      "label": "Disable if any mod active",
      "description": "This comp is disabled when any mod in this list is loaded.",
      "type": { "kind": "list" },
      "items": { "kind": "string" },
      "required": false,
      "xml": "listOfLi"
    },
    "incident": {
      "label": "Incident",
      "description": "IncidentDef fired once by this comp.",
      "type": { "kind": "defReference" },
      "required": false,
      "xml": "element",
      "reference": { "defType": "IncidentDef", "allowAbstract": false, "scope": "allSources" }
    },
    "fireAfterDaysPassed": {
      "label": "Fire after days passed",
      "description": "Number of in-game days after which the incident fires.",
      "type": { "kind": "integer" },
      "required": false,
      "xml": "element"
    },
    "skipIfColonistHasMinTitle": {
      "label": "Skip if colonist has min title",
      "description": "Skip firing if any colonist already holds at least this RoyalTitleDef rank.",
      "type": { "kind": "defReference" },
      "required": false,
      "xml": "element",
      "reference": { "defType": "RoyalTitleDef", "allowAbstract": false, "scope": "allSources" }
    },
    "skipIfOnExtremeBiome": {
      "label": "Skip if on extreme biome",
      "description": "If true, the incident is skipped when the colony is on a biome flagged as extreme.",
      "type": { "kind": "boolean" },
      "required": false,
      "defaultValue": false,
      "xml": "element"
    },
    "minColonistCount": {
      "label": "Min colonist count",
      "description": "Minimum number of colonists required for this comp to fire.",
      "type": { "kind": "integer" },
      "required": false,
      "defaultValue": 0,
      "xml": "element"
    }
  }
}
